rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only signed-in users (you + your father's accounts) can read/write photos.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
